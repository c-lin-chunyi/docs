{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run generate:radix-css && node scripts/build-sites.mjs",
    "generate:radix-css": "node scripts/generate-radix-css.mjs",
    "preview": "python3 -m http.server 8000 --directory dist",
    "serve": "npm run build && npm run preview",
    "start": "npm run serve"
  },
  "devDependencies": {
    "@radix-ui/colors": "^3.0.0"
  }
}
